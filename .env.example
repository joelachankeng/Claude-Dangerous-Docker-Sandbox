# Copy this file to .env and fill in the token.
# Generate the token by running inside the container:
#   claude setup-token
#
# .env is git/docker-ignored — never commit the real token.
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-replace-me
